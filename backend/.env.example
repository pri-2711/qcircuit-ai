# Optional. Leave both unset and the app still works fully -- /api/explain and
# /api/chat fall back to a template-based explainer grounded in the real
# circuit analysis JSON (no hallucinated gates/probabilities, just less
# free-form than a real LLM).
#
# Set ONE of these to upgrade explanations/chat to a real LLM call:
OPENAI_API_KEY=
GEMINI_API_KEY=
