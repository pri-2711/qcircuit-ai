# Copy this file to backend/.env and set your key there.
# The key is read only by the backend and never exposed to the frontend.
# Leave it empty to keep using the built-in template tutor.
OPENAI_API_KEY=
GEMINI_API_KEY=
